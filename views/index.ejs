<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>Weather & Air Quality</title>

  <style>
    * {
      margin: 0;
      padding: 0;
      box-sizing: border-box;
      font-family: "Poppins", sans-serif;
    }

    body {
      height: 100vh;
      width: 100%;
      display: flex;
      justify-content: center;
      align-items: center;
      position: relative;
      overflow: hidden;
      background-size: cover;
      background-position: center;
      background-repeat: no-repeat;
    }

    .overlay {
      position: absolute;
      top: 0;
      left: 0;
      height: 100%;
      width: 100%;
      background-color: rgba(0, 0, 0, 0.45);
      z-index: 0;
    }

    .content-box {
      position: relative;
      z-index: 1;
      background: rgba(255, 255, 255, 0.85);
      border-radius: 20px;
      padding: 2.5rem 3rem;
      text-align: center;
      box-shadow: 0 10px 25px rgba(0, 0, 0, 0.3);
      max-width: 420px;
      width: 90%;
      backdrop-filter: blur(5px);
    }

    h1 {
      font-size: 2rem;
      color: #333;
      margin-bottom: 1rem;
    }

    form {
      display: flex;
      justify-content: center;
      gap: 10px;
      margin-bottom: 1.5rem;
    }

    input[type="text"] {
      padding: 0.7rem 1rem;
      border: 1px solid #ccc;
      border-radius: 25px;
      width: 70%;
      font-size: 1rem;
      transition: 0.3s;
    }

    input[type="text"]:focus {
      outline: none;
      border-color: #007bff;
      box-shadow: 0 0 8px rgba(0, 123, 255, 0.4);
    }

    button {
      padding: 0.7rem 1.2rem;
      background: #007bff;
      border: none;
      color: white;
      font-size: 1rem;
      border-radius: 25px;
      cursor: pointer;
      transition: 0.3s;
    }

    button:hover {
      background: #0056b3;
    }

    .result {
      margin-top: 1rem;
      color: #333;
    }

    .result h2 {
      font-size: 1.5rem;
      margin-bottom: 0.5rem;
    }

    .result p {
      font-size: 1rem;
      margin: 0.3rem 0;
    }

    .error {
      color: red;
      font-weight: bold;
    }

    .placeholder {
      color: #555;
      font-style: italic;
    }
  </style>
</head>

<body 
  <% if (cityImage) { %>
    style="background-image: url('<%= cityImage %>');"
  <% } else { %>
    style="background: linear-gradient(135deg, #89f7fe 0%, #66a6ff 100%);"
  <% } %>
>
  <div class="overlay"></div>

  <div class="content-box">
    <h1>🌤 Weather & Air Quality</h1>

    <form action="/get-data" method="POST">
      <input type="text" name="city" placeholder="Enter city name..." required />
      <button type="submit">Check</button>
    </form>

    <div class="result">
      <% if (error) { %>
        <p class="error"><%= error %></p>
      <% } else if (data) { %>
        <h2><%= data.name %>, <%= data.country %></h2>
        <p>🌡 Temperature: <b><%= data.temperature %>°C</b></p>
        <p>💨 Windspeed: <b><%= data.windspeed %> km/h</b></p>
        <p>🌫 PM10: <b><%= data.pm10 %></b></p>
        <p>🌫 PM2.5: <b><%= data.pm25 %></b></p>
      <% } else { %>
        <p class="placeholder">Start by entering a city name above 👆</p>
      <% } %>
    </div>
  </div>
</body>
</html>
